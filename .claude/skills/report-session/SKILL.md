---
name: report-session
description: セッション中に得た知見・調査結果・設計判断・実装結果を整理し、後から参照できる Markdown ドキュメントとして保存するスキル。「まとめて」「記録して」「ドキュメントにして」「文書化して」「知見をまとめて」「わかったことを整理して」「レポートにして」「保存して」など、このセッションの内容を新しい永続ドキュメントとして残す依頼ならすべて使用する。保存先は Obsidian（CLI あり）または `__doc/` / `~/Documents`。PR説明文・コミットメッセージ・リリースノート・既存ファイルの要約・コードの説明には使わない。
---

# セッションレポート作成

## 内容の規約

### SHOULD

- 人間やAIのためのナレッジとして蓄積、参照可能な文書にしてください
- 抽象化された体系的な構造を抽出して、それを具体的な情報や例で説明してください
- 難解な構造があれば Mermaid 形式の図を併記してください
- 全体を通して得られた気づきや学び、学習のための汎用的な情報があれば、最後に独立したセクションで記載してください

### MUST NOT

- 過剰な箇条書きは避けてください。抽象的になりすぎるおそれがあれば、わかりやすく文章で説明してください
- SHOULD の項目を無理に実行して不自然な文章を作らないでください
- このレポート作成行為自体はレポートに含めないでください。レポート作成は複数回実行することがあります

## 出力先の決定

- TIMESTAMP: `date "+%Y%m%d_%H%M%S"` で取得
- TITLE: レポートの第一見出し（`# ...`）をそのまま使う。ただしファイル名に使えない文字（`/\:*?"<>|`）は除去またはハイフンに置換する
- ファイル名: `TIMESTAMP TITLE.md`（TIMESTAMP と TITLE の間はスペース）
- フォルダ: 常に `claude-report/`

### Obsidian CLI が使える場合

`which obsidian` で確認する。コマンドが存在すれば有効とみなす。
（アプリが起動していなければ最初のコマンド実行時に自動起動する）

1. レポート内容を生成し、`/tmp/report-session.md` に書き出す（Write ツール）
2. 以下のコマンドで vault の `claude-report/` フォルダに作成する:

```bash
ENCODED=$(python3 <skill-dir>/scripts/obs_encode.py /tmp/report-session.md)
obsidian create path="claude-report/TIMESTAMP TITLE.md" content="$ENCODED" overwrite
```

vault 指定なし → カレントディレクトリか Obsidian のデフォルト vault が自動使用される。

### Obsidian CLI がない場合

- パス: `__doc/` あるいは `~/Documents` を選択（パスの指定があればそれに従う）
- `claude-report/` サブフォルダに保存する

## 出力後のレビュー

出力後は内容を読み返してレビューしてください。
信頼性の低い情報や不正確な情報があれば、追加で調査した上で内容を更新してください。
