---
name: report-session
description: セッション中に得た知見・調査結果・設計判断・実装結果を整理し、後から参照できる Markdown ドキュメントとして保存するスキル。「まとめて」「記録して」「ドキュメントにして」「文書化して」「知見をまとめて」「わかったことを整理して」「レポートにして」「保存して」など、このセッションの内容を新しい永続ドキュメントとして残す依頼ならすべて使用する。保存先は Obsidian（CLI あり）または `__doc/` / `~/Documents`。PR説明文・コミットメッセージ・リリースノート・既存ファイルの要約・コードの説明には使わない。
---

# セッションレポート作成

## 実行モード

**デフォルトは、会話履歴を継承する fork への委譲**。利用する環境のサブエージェント機能で起動する。ユーザーがインライン実行を指定した場合、または履歴を継承する fork が利用できない場合は、この会話で実行する。

1. 更新か分割かなど、ユーザーの判断が必要な事項を委譲前に確認する。
2. 委譲先には、このスキルの規約・ユーザーの指定・決定済みの保存先を伝え、本文生成から保存・リンク・学びキュー登録・出力後レビューまで任せる。一時ファイルは `/tmp/rs-TIMESTAMP.md`、保存は `scripts/save_report.rb` を使う。
3. メインはバックグラウンドで作成中と伝え、元の作業を続ける。委譲先は保存先・タイトル・一行サマリを返す。未完了なら完了した工程とブロック内容を返す。
4. 未完了の場合は原因を解消して同じエージェントで再開する。新規起動による二重保存を避け、成果物を確認してから完了を報告する。

## 内容の規約

レポートは **ヘッダー → 本文 → フッター** の3区分で構成する。目次を厳密に固定する意図はなく、各区分に「何を置くか」の役割を与えるだけ。各区分内の項目は、該当する情報・出来事が無ければ省略してよい。

### 全体方針

- MUST: 人間やAIのためのナレッジとして蓄積・参照可能な文書にする
- MUST NOT: 過剰な箇条書きは避ける。抽象的になりすぎるおそれがあれば、わかりやすく文章で説明する
- MUST NOT: 規約の項目を無理に埋めて不自然な文章を作らない（該当が無ければ省略する）
- MUST NOT: このレポート作成行為自体はレポートに含めない（レポート作成は複数回実行することがある）

### ヘッダー

- SHOULD: 背景・目的を記載し、何のためのセッションだったかを最初に示す

### 本文

- SHOULD: 抽象化された体系的な構造を抽出し、それを具体的な情報や例で説明する
- SHOULD: 難解な構造があれば Mermaid 形式の図を併記する

### フッター

セッションを締めくくる学びを、**性質ごとに独立したセクション**として並置する。下に行くほど題材から離れ、転用範囲が広がる順に並べる。

`普遍的な学び` と `エージェントワークフローの学び` の各項目（トップレベルの箇条書き）には、行末にそれぞれ `#learn/knowledge` / `#learn/agent` タグを付ける。レポートを跨いだ学びの横断検索（Obsidian のタグ検索・grep）を項目単位で可能にするため。ネストした補足の箇条書きには付けない。

さらに各項目のタグの直前に、学びマップの既存テーマへの事前クラスタリング注記 `（map: テーマ名）` を付ける（`/learning-review` がテーマ単位のマージだけで済むようにするため）:

- テーマ一覧は `grep '^####' "$VAULT/claude-report/_learning-map.md"` で取得する（1 コマンドで足りる。マップ本文は読まない）
- テーマ名は見出しから正確に写す（造語しない）。見出しが「主部 — 補足」形式なら主部だけでよい
- 合う既存テーマが無ければ `（map: 新規候補）` とする。迷ったら新規候補に倒す（誤合流よりレビュー時に判断させる方が安全）
- マップが存在しない・vault が使えない場合はこの注記ごと省略する

例: `- 検証コマンドは既知の陽性ケースで先に発火確認する（map: 検証の証拠主義） #learn/agent`

- SHOULD: 題材について全体を通して得られた気づきと結論のまとめ（この具体ケースで何が分かったか）
- SHOULD: `## 普遍的な学び` — 他の同種の問題にも転用できる知識・原則（題材を離れても成り立つ事実）。**「知識・原則」**であって「エージェントの振る舞い」ではない
- MUST: `## エージェントワークフローの学び` — 本セッション中に観測された「次回以降の AI エージェントの振る舞いに反映できそうな学び」を**必ず検討する**。題材の内容ではなく**エージェントの働き方**に関するメタな学び。CLAUDE.md / `.claude/rules/` / skills に落とし込めそうな粒度を意識する
  - 含める観点の例:
    - ユーザーがエージェントの判断・出力を訂正した（何をどう直されたか、なぜか）
    - エージェントの前提知識が古い・間違っていた（実際の正解と、どこで気付いたか）
    - 試行錯誤の末により良い手順・ツール・コマンドを見つけた
    - コマンド・ツール実行が失敗した（原因と、回避・代替策）
    - スキル / hook / 設定の不足や穴に気付いた
  - **必ず検討する**が MUST であって、記載は任意。該当する出来事が無ければセクションごと省略してよい（無理に書かない）
  - すぐ上の「普遍的な学び」（知識・原則）とは別物。あちらが**「知識・原則」**、こちらが**「エージェントの振る舞い・やり方」**という違いで並置する

## 2回目以降の起動（同一セッション内）

すでにこのセッションで `/report-session` を実行してレポートを出力済みの場合、新たな内容を追記する前に **利用可能な対話手段で以下をユーザーに選んでもらう**:

1. **既存レポートを更新** — 直前のレポートファイルを Read してから Edit で追記・修正する。タイトルの主題から外れる追加は行わない。Obsidian 保存で学びの項目を追記・変更した場合は `ruby <skill-dir>/scripts/append_to_triage.rb "TIMESTAMP TITLE"` を再実行してキューの件数を更新する
2. **別レポートとして切り出し** — 新規ファイルを作成し、**両レポートに相互リンクを貼る**:
   - 新レポートの冒頭（frontmatter 直後・`# TITLE` の前後どちらでも可）に `> 関連: [[既存レポートのファイル名（拡張子なし）]]` を入れる
   - 既存レポートにも同じ要領で `> 関連: [[新レポートのファイル名（拡張子なし）]]` を Edit で追記する
   - 双方向リンクが揃ったことを確認してから、新レポートの daily note リンク追記を行う

迷うようなら「主題が同じで深掘り or 補足 → 更新」「別の主題・別の切り口 → 分割」が目安。判断材料が無ければユーザーに選ばせる（Auto Mode 中でも本選択はスキップしない）。

## タグ

レポート冒頭に YAML frontmatter で `tags:` を付ける。**最大3つまで**。記事の中心的な位置付けを表すタグに絞る（3つに限定することで焦点のブレを防ぐのが目的なので、無理に3つ埋めず2つや1つでもよい）。

- **Obsidian 保存では既存タグを優先する**: `ruby <skill-dir>/scripts/obsidian.rb tags` で vault の既存タグ一覧を取得し、内容に合うものをそこから選ぶ。該当が無いとき、またはローカル保存の場合は kebab-case で作成する。
- 表記は kebab-case（例: `agentic-coding`, `google-cloud`, `flaky-test`）。
- `claude-report` のような分類マーカーは付けない（記事の位置付けを表すトピックタグのみ）。

frontmatter は本文の `# TITLE` の前に置く（TITLE 抽出には影響しない）:

```yaml
---
tags:
  - obsidian
  - cli
  - skill-design
---
```

## 出力先の決定

- TIMESTAMP: `date "+%Y%m%d_%H%M%S"` で取得
- TITLE: レポートの第一見出し（`# ...`）。**そのままファイル名・wikilink ターゲットになる**ことを前提に、最初から記号に頼らない抽象化されたタイトルを付ける:
  - 内容の本質を言葉で表す（主題＋切り口）。issue 番号・PR 番号・ファイルパス・コマンド名などの識別子はタイトルに入れず、本文に書く（例: `flaky test #29915 の原因切り分け` ではなく `flaky test の原因を instrumentation で確定させる`）
  - 次の文字は使わない: `# ^ [ ] |`（Obsidian の wikilink を壊す。特に `#` は見出しアンカー扱いになりリンクが解決しなくなる）、および `/ \ : * ? " < >`（OS でファイル名に使えない）
  - 区切りが要るときは全角（`：` `・` `—`）や半角スペースを使う。連続スペースは作らない
  - このルールで付けたタイトルは変換不要でそのままファイル名にできる。**もし記号が残ってしまった場合のみ**、保険として除去してからファイル名にする（`tr -d '#^[]|\\/:*?"<>'`）
- ファイル名: `TIMESTAMP TITLE.md`（TIMESTAMP と TITLE の間はスペース）
- フォルダ: 常に `claude-report/`

### 保存の手順

ユーザーがローカル保存を指定している場合は Obsidian の接続確認・タグ取得・学びマップ参照を省略し、その保存先で下記の `--local` コマンドを使う。本文生成と出力後レビューは保存先によらず行う。

1. ローカル保存の指定がない場合は `ruby <skill-dir>/scripts/obsidian.rb check` で接続を確認する。終了コード 0 はアクセス可能な vault の JSON、2 は CLI 未導入、1 は接続・設定エラー。コマンドはアプリを起動する場合がある。
   - 0: JSON の `vault` を `VAULT` として学びマップの参照に使う。複数 vault がある場合は `OBSIDIAN_VAULT` に対象名または ID を指定し、以降のコマンドでも同じ値を使う。
   - 2: 保存先の指定を優先し、未指定なら `__doc/` または `~/Documents` を選ぶ。
   - 1: 原因を報告して停止する。接続障害を理由に別の場所へ黙って保存しない。
   - WSL で接続できない場合に限り [セットアップ手順](references/setup-wsl.md) を読む。
2. Obsidian 保存の場合は既存タグを取得する。保存先によらず frontmatter と本文を `/tmp/rs-TIMESTAMP.md` に作成する。
3. Obsidian 保存には次のコマンドを使う。`TIMESTAMP TITLE` は最終ファイル名の拡張子を除いた部分で、再実行でも同じ値を使う。

```bash
ruby <skill-dir>/scripts/save_report.rb /tmp/rs-TIMESTAMP.md "TIMESTAMP TITLE"
```

Markdown を直接保存し、今日の既存 daily note の `## Claude Reports` にリンクを挿入し、学びがあれば `claude-report/_learning-triage.md` に登録する。daily note が未作成の場合は保存済みレポートを示して失敗する。既存の作成方法で daily note を用意してから同じコマンドを再実行する。CLI の本文引数は多層のエスケープで内容を壊しうるため使わない。

CLI 未導入時、またはユーザーがローカル保存を指定した場合:

```bash
ruby <skill-dir>/scripts/save_report.rb --local __doc /tmp/rs-TIMESTAMP.md "TIMESTAMP TITLE"
```

指定ディレクトリの `claude-report/` に保存し、Obsidian へのリンク・キュー登録は行わない。

既存ファイルの内容が異なる場合は上書きせず失敗する。同じ内容ならリンク・キュー登録を再開できる。失敗時は保存済みパスと未完了の工程を報告する。既存レポートの更新は内容を確認して直接編集し、Obsidian 保存時は `ruby <skill-dir>/scripts/append_to_triage.rb "TIMESTAMP TITLE"` を再実行する。チェック済みの学びは再レビュー用の未チェック行になる。

## 出力後のレビュー

出力後は内容を読み返してレビューしてください。
信頼性の低い情報や不正確な情報があれば、追加で調査した上で内容を更新してください。
